seed: 23

data:
  manifest: data/hangul/manifest.jsonl
  val_ratio: 0.12
  num_workers: 0

model:
  input_channels: 7
  base_channels: 16
  depth: 3
  dropout: 0.04

loss:
  bce_weight: 1.0
  dice_weight: 0.8
  ink_weight: 0.65
  skeleton_weight: 0.0
  smooth_weight: 0.05

training:
  output_dir: runs/hangul
  epochs: 18
  batch_size: 8
  learning_rate: 0.0008
  weight_decay: 0.00001
  device: auto
  amp: false
