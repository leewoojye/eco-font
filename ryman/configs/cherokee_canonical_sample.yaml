seed: 43

data:
  manifest: data/cherokee_canonical_sample/manifest.jsonl
  val_ratio: 0.15
  num_workers: 0
  target_style: canonical

model:
  input_channels: 7
  base_channels: 16
  depth: 3
  dropout: 0.05

loss:
  bce_weight: 1.0
  dice_weight: 0.85
  ink_weight: 1.0
  skeleton_weight: 0.0
  smooth_weight: 0.025

training:
  output_dir: runs/cherokee_canonical_sample
  epochs: 20
  batch_size: 8
  learning_rate: 0.0008
  weight_decay: 0.00001
  device: auto
  amp: false
