language: ko
image_size: 128
targets: [0.15, 0.25, 0.35]
rule_optimizer:
  readability_weight: 2.0
  ink_weight: 0.4
  target_weight: 2.5
  topology_weight: 3.0
model:
  input_channels: 4
  base_channels: 32
  output_channels: 1
training:
  epochs: 10
  batch_size: 16
  learning_rate: 0.001
